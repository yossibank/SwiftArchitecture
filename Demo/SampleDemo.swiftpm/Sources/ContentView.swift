import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(.information)
            Text("Hello, world!")
        }
    }
}

#Preview {
    ContentView()
}
